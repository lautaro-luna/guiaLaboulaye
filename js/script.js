
import $ from 'jquery';

const card = document.getElementById('tajeta');

card.addEventListener.addEventListener("mouseover", function() {
    
  });

  document.addEventListener('DOMContentLoaded', function () {
    var myCarousel = new bootstrap.Carousel(document.getElementById('carrusel'), {
      interval: 1000, // Cambia el intervalo de transición en milisegundos (opcional)
      wrap: true // Si deseas que el carrusel vuelva al principio después de llegar al final (opcional)
    });
  });
  